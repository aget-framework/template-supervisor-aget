# Agent Configuration

## Project Context
my-AGET-template - AGET v2.0.0 compliant agent template

## Session Management Protocols

### Wake Up Protocol
When user says "wake up" or "hey":
- Show current directory and status
- Check git status
- Report readiness

### Wind Down Protocol
When user says "wind down" or "save work":
- Commit changes
- Create session notes
- Show completion

### Sign Off Protocol
When user says "sign off" or "all done":
- Quick save and exit
- No questions

## Directory Structure
- `.aget/` - Framework metadata and configuration
- `.aget/evolution/` - Decision and discovery tracking
- `.aget/checkpoints/` - Agent state checkpoints
- `src/` - Agent source code
- `workspace/` - Internal workspace for explorations
- `products/` - Public products the agent maintains
- `data/` - Persistent data storage
- `docs/` - Documentation
- `tests/` - Test suite

## Vocabulary Note
- `workspace/` = Your agent's private workspace for exploration
- `products/` = Public products your agent creates/maintains for others
- `src/` = Source code for your agent/tool
- `.aget/evolution/` = Decision and discovery tracking

## Substantial Change Protocol
For changes affecting >3 files or major refactoring:
1. Create detailed plan with Task tool
2. Present plan for approval
3. Execute incrementally with verification

## GitHub Issues Integration
- **Hub Repository**: `aget-framework/aget`
- **Patterns**: `.aget/patterns/github/` for issue operations
- **Routing**: Use `agent_discovery.py` for cross-agent coordination
- **Issue Tags**: Use `[agent-name]` prefix for agent-specific issues

## Available Patterns
All patterns located in `.aget/patterns/`:
- `session/` - Session management (wake_up, wind_down, sign_off)
- `github/` - GitHub issue operations
- `routing/` - Agent discovery and coordination
- `documentation/` - Smart document reading
Run `ls .aget/patterns/` to see all available patterns.

## Efficiency Rules
- **>3 similar operations**: Use Task tool or batch operations
- **"read docs"**: Use `.aget/patterns/documentation/smart_reader.py` (local) or Task tool
- **Multiple file operations**: Batch with Task tool
- **Pattern location**: All patterns in `.aget/patterns/` (v2.0 standard)
- **Check patterns**: `.aget/patterns/session/EFFICIENT_STARTS.md` for optimal approaches
- **Self-contained**: Per ARCH-001, all patterns must be installed locally

## Compliance
- **Version**: 2.0.0 (full compliance)
- **Structure**: v2.0 standard directories
- **Patterns**: All in `.aget/patterns/`
- **Protocols**: Session, GitHub, and routing enabled

---
*Generated by AGET v2 - https://github.com/aget-framework/aget*
